{
  "name": "pawelca-apps-site",
  "private": true,
  "type": "module",
  "scripts": {
    "docs:dev": "vitepress dev docs-eventmonitor",
    "docs:build": "vitepress build docs-eventmonitor",
    "docs:preview": "vitepress preview docs-eventmonitor",
    "docs:search": "pagefind --site public/eventmonitor",
    "docs:build:all": "npm run docs:build && npm run docs:search"
  },
  "devDependencies": {
    "pagefind": "^1.3.0",
    "vitepress": "^1.6.3"
  }
}
